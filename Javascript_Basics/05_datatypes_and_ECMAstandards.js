"use strict" // it treats all the JS code like written in newer version of JS

// alert("Hello Ji");



/*
--> ECMA is a standard documentation where we get details about JavaScript language
--> Some Datatypes
    --> number = 2^53
    --> bigint
    --> string
    --> boolean
    --> null => standalone value {let state = null;}
    --> undefined => no value   {let state;}
    --> symbol => unique identofier
            A Symbol is a primitive data type introduced in ES6. It's used to create unique identifiers — even if they have the same description.

            const a = Symbol("id");
            const b = Symbol("id");
            console.log(a === b); // false ❌ — because every Symbol is unique
    
    Important.....
    --> datatype of null is object, console.log(typeof null) ;
    --> datatype of undefined is undefined,console.log(typeof      undefined);

*/